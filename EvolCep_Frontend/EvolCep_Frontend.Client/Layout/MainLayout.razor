@inherits LayoutComponentBase
@using EvolCep_Frontend.Client.Services
@inject AuthService AuthService
@inject NavigationManager Navigation

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="top-row px-4 d-flex justify-content-end align-items-center">
            <AuthorizeView>
                <Authorized>
                    <span class="me-3 text-muted">
                        Hola, <strong>@context.User.Identity?.Name</strong>
                    </span>
                    <button class="btn btn-sm btn-outline-danger" @onclick="HandleLogout">
                        Cerrar Sesión
                    </button>
                </Authorized>
                <NotAuthorized>
                    <a href="login" class="btn btn-sm btn-primary">Iniciar Sesión</a>
                </NotAuthorized>
            </AuthorizeView>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

@code {
    private async Task HandleLogout()
    {
        await AuthService.LogoutAsync();
        Navigation.NavigateTo("login", forceLoad: true); 
        // forceLoad: true asegura que la app se refresque y limpie cualquier estado viejo
    }
}
